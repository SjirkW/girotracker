{
  "name": "girotracker",
  "private": true,
  "scripts": {
    "degiro": "concurrently -n server,web -c blue,magenta --kill-others-on-fail \"npm run dev --prefix server\" \"npm run dev --prefix web\"",
    "install:all": "npm install --prefix server && npm install --prefix web",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "wrangler": "^4.85.0"
  }
}
